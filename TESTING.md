# 测试指南

## 本地自动化验证

```bash
. .venv/bin/activate
pytest -o addopts= -q
```

测试使用 moto 和假客户端，不操作真实 AWS 资源或外部探测服务。Web server 测试监听本机回环地址；运行环境需要允许本地端口监听。页面状态测试使用 Node.js 执行实际 JavaScript，并模拟 DOM；没有 Node.js 时该项显示 skipped。

浏览器交互测试使用本机 Chrome 或 Chromium，连接隔离的模拟 HTTP 服务；可用 `CHROME_BIN` 指定浏览器路径，未找到浏览器时显示 skipped。截图和浏览器日志写入 pytest 临时目录。

通过数量由本次测试输出确定，不使用文档中的固定历史数字代替测试结果。

核心验证覆盖：

- AWS 区域与机型分页、可用区筛选、动态规格和不同配额组。
- 机型名称不等待全部规格返回；加载中、失败重试及同一区域和子网缓存回退；结果到达时保留用户输入。
- 多机型数量合计、每轮 50 台上限、共享/独立配额组、混合架构镜像、不同可用区子网及后续机型启动失败的清理。
- 每轮启动数量与最终保留数量分别配置；高分但数量不足时继续轮次，浏览器模拟流程验证每轮 6 台、2 轮后保留 7 台。
- 完整台数请求、逐台 AMI/系统盘覆盖、镜像不兼容及分组启动失败后的清理。
- 常用镜像的区域和架构匹配、不可用镜像排除、跨区域缓存隔离及查询失败后的重试。
- Python 示例默认不创建、显式执行时传递配置、连接中断后不重复创建。
- 逐台继承默认镜像、切换区域后阻止使用过期查询结果。
- GitHub 名单 IP/CIDR 命中，空文件或 HTML 不能通过检查，默认排除检查失败的候选。

- 配置数值、权重、探测源最低数量与目标格式校验。
- 外部 ICMP 探测安全组、反向探测专用组以及自定义组检查。
- 全丢包、SSM 离线、覆盖不足不能被误选为保留实例。
- 保留标签写入失败后仍可按运行 ID 查找；成功保留的实例被清理排除。
- 共享 IAM 所有权、跨区域依赖和查询失败时禁止删除。
- HTTP 超时、WebSocket 空闲后的后续样本、信誉未知状态。
- 候选级错误与样本写入报告，密钥在配置和错误文本中脱敏。
- 停止保护失败不被报告为成功，不继续终止其他候选。
- 远程同源校验，选定与后续终止状态的持久化。
- 历史运行命令的区域、未知运行的清理入口和结果状态展示。

## 计划验证

```bash
scripts/find_best_instance.sh ap-east-1 2 1 1 --dry-run
```

检查数量、区域、探测源和入站要求。默认启用外部探测时，应出现 `crossborder-selector-ping-sg` 和 IPv4 ICMP Echo Request 说明；不应创建客户端或 AWS 资源。

## 模拟 Web 流程

```bash
scripts/start_web.sh --demo
```

1. 选择 3 台候选、1 轮、保留 2 台，查看计划后开始。
2. 检查结果、信誉状态与探测明细，并选定一台，暂不终止其他实例。
3. 在完成页终止其余保留候选，刷新后确认不再显示待清理。
4. 打开其他区域的历史运行，核对生成命令中的 `--region`。
5. 下载 JSON，检查 `schema_version: 2`、`probe_results` 和 `reputation_results`。

模拟数据用于检查操作流程，不验证真实网络质量或 AWS 权限。

配置交互还应验证：机型列表可滚动至末尾、输入搜索、方向键与 Enter 选中、窄屏无横向溢出；默认与逐台设置都可选系统或手动输入 AMI；切换 ARM/x86 机型时，所选系统重新解析为兼容镜像。Python 模拟调用命令见 [Python 使用方法](docs/python-api.md)。

多机型浏览器验证：添加 `t3.nano × 2`、`t4g.nano × 3`；为第二组的一台实例设置不同系统和磁盘；增减第一组数量、移除第一组后核对该设置仍属于第二组；模拟运行报告中两种机型数量应分别与提交清单一致。

## 用户侧真实 AWS 测试

真实运行会创建并保留实例，需由测试者确认区域、权限、费用和清理范围。

```bash
scripts/find_best_instance.sh ap-east-1 2 1 1
```

建议逐项核对：

1. 默认探测组只新增 IPv4 ICMP Echo Request 入站规则，没有 TCP/UDP 入站端口。
2. 在 JSON 中核对发送数、接收数、丢包率与平均时延；不要只看综合分。
3. 保留实例有运行归属标签和 `crossborder-winner=true`；其余候选已终止。
4. 普通 cleanup 不终止保留实例，且在真实 API 返回错误时有明确反馈。
5. 使用 `--protect` 时，在 EC2 分别核对停止和终止保护。
6. 在需要验证中断恢复的场景停止服务进程；重启后通过“查询并清理本次候选”核查残留。
7. 业务接入前从实际目标用户网络进行业务测试，避免把公共 ping 得分作为业务可用性结论。

不要为验证跨区域保护而直接删除生产 IAM 资源；该失败路径已有 moto 回归测试。
